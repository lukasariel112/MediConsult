import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import LoginApp from './LoginApp'; // A nova aplicação de login

ReactDOM.render(<App />, document.getElementById('root'));
